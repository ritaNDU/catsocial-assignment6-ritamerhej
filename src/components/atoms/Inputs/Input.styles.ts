import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '100%',
    marginBottom: 10,
    padding: 10,
    paddingLeft: 20,
    borderWidth: 0.5,
    borderRadius: 10,
  },
});

export default styles;
