import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  captureButton: {
    borderWidth: 5,
    borderColor: 'gray',
    backgroundColor: '#fff',
    borderRadius: 100,
    height: 100,
    width: 100,
  },
});

export default styles;
